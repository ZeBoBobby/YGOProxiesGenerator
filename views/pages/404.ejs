<!doctype html>
<html lang="<%= lang.includes('fr') ? 'fr' : 'en' %>">
  <head>
    <% 
      const pageTitle = lang.includes('fr') ? '404 - Page non trouvée - YGOProxy.com' : '404 - Page not found - YGOProxy.com';
      const pageDescription = lang.includes('fr') ? 'Page non trouvée' : 'Page not found';
    %>
    <%- include('../partials/head', { title: pageTitle, description: pageDescription, canonical: 'https://ygoproxy.com' }); %>
    <meta name="robots" content="noindex, follow"> 
    <style>
      .error-404 {
        text-align: center;
        min-height: 50vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }

      .error-404-icon {
        font-size: 6rem;
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <%- include('../partials/header'); %> 

    <div class="wrapper">
      <div class="content-box error-404">
        <div class="error-404-icon">🔍</div>
        <h1><%= lang.includes('fr') ? 'Erreur 404 - La page demandée n\'existe pas !' : '404 error - The requested page does not exist!' %></h1>
        <p style="color: var(--text-secondary);"><%= lang.includes('fr') ? 'Prends soin de toi et ' : 'Keep safe and ' %><a href="/"><%= lang.includes('fr') ? 'retourne à la maison' : 'go back to home' %></a> 🏠</p>
      </div>
    </div>

    <%- include('../partials/footer'); %> 
  </body>
</html>

